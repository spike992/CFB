CAN_french_canadian_conscription_crisis_category = {

	CAN_political_concessions_to_french_canadians = {

		icon = GFX_decision_hol_exchange_intelligence_data

		allowed = {
			original_tag = CAN
		}
	
		available = {
			FROM = {
				NOT = { has_state_flag = CAN_currently_offering_concessions }
			}
		}

		state_target = yes
		target_trigger = {
			FROM = {
				is_controlled_by = ROOT
				has_state_flag = CAN_conscription_crisis_flag
			}
		}
		on_map_mode = map_and_decisions_view

		visible = {
			has_completed_focus = CAN_solve_the_conscription_crisis
		}

		ai_will_do = {
			base = 50
		}

		cost = 20
	
		days_remove = 30

		modifier = {
			political_power_cost = 0.1
		}

		complete_effect = {
			FROM = {
				set_state_flag = {
					flag = CAN_currently_offering_concessions
					days = 30
					value = 1
				}
			}
		}
	
		remove_effect = {
			FROM = {
				CAN_lower_conscription_crisis = yes
			}
		}
	}

	CAN_industrial_concessions_to_french_canadians = {

		icon = GFX_decision_generic_industry

		allowed = {
			original_tag = CAN
		}
	
		available = {
			FROM = {
				NOT = { has_state_flag = CAN_currently_offering_concessions }
			}
		}

		state_target = yes
		target_trigger = {
			FROM = {
				is_controlled_by = ROOT
				has_state_flag = CAN_conscription_crisis_flag
			}
		}
		on_map_mode = map_and_decisions_view

		visible = {
			has_completed_focus = CAN_solve_the_conscription_crisis
		}

		ai_will_do = {
			base = 50
		}

		days_remove = 30

		modifier = {
			consumer_goods_factor = 0.05
		}

		complete_effect = {
			FROM = {
				set_state_flag = {
					flag = CAN_currently_offering_concessions
					days = 30
					value = 1
				}
			}
		}
	
		remove_effect = {
			FROM = {
				CAN_lower_conscription_crisis = yes
			}
		}
	}

	CAN_military_concessions_to_french_canadians = {

		icon = GFX_decision_generic_military

		allowed = {
			original_tag = CAN
		}
	
		available = {
			FROM = {
				NOT = { has_state_flag = CAN_currently_offering_concessions }
			}
		}

		state_target = yes
		target_trigger = {
			FROM = {
				is_controlled_by = ROOT
				has_state_flag = CAN_conscription_crisis_flag
			}
		}
		on_map_mode = map_and_decisions_view

		visible = {
			has_completed_focus = CAN_send_in_the_zombies
		}

		ai_will_do = {
			base = 50
		}

		days_remove = 30

		custom_cost_text = CAN_decision_cost_army_experience

		custom_cost_trigger = {
			has_army_experience > 9
		}

		days_remove = 30

		complete_effect = {
			army_experience = -10
			FROM = {
				set_state_flag = {
					flag = CAN_currently_offering_concessions
					days = 30
					value = 1
				}
			}
		}
	
		remove_effect = {
			FROM = {
				CAN_lower_conscription_crisis = yes
			}
		}
	}

	
}